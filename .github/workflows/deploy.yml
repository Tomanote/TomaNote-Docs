name: Deploy to GitHub Pages

on:
  push:
    branches: [ master ]
    paths-ignore:
      - "dist/**"
      - ".github/**"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: deploy-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - run: npm run build

      - name: Prepare GitHub Pages files
        run: |
          touch dist/.nojekyll
          echo "docs.tomanote.app" > dist/CNAME

      - uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist
